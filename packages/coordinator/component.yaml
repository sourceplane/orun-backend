apiVersion: sourceplane.io/v1
kind: Component

metadata:
  name: orun-coordinator

spec:
  type: turbo-package
  domain: orun-backend
  subscribe:
    environments:
      - dev
      - staging
      - production
  inputs:
    nodeVersion: "20"
    pnpmVersion: "10.12.1"
  labels:
    team: platform
    layer: shared
    surface: coordinator
    runtime: cloudflare
