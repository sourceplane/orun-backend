# Proposal

Keep Dashboard UI blocked until remote-state conformance and local filesystem locking are proven in `sourceplane/orun`.

# Found By

Orchestrator after Task 0008.3 live backend deployment.

# Related Task

- Task 0008.4 - orun local-state locking and concurrency proof
- Task 0008.5 - orun remote-state matrix conformance and testing docs

# Current Spec Text / Contract

`spec/09-cli-integration.md` requires:

- `orun run <planID> --remote-state --job <jobID>` to work in GitHub Actions matrix execution.
- Jobs whose dependencies are incomplete to wait by polling remote state.
- Duplicate job targets to exit cleanly when already running or complete.
- A copyable GitHub Actions matrix example.
- `orun status --remote-state` and `orun logs --remote-state`.
- Local filesystem state to remain the default and to be best-effort safe when multiple local processes share the same `--exec-id`.

# Repo Reality / New Information

Task 0008.3 deployed the backend live at:

```text
https://orun-api.rahulvarghesepullely.workers.dev
```

However, the `sourceplane/orun` remote-state conformance workflow failed before reaching backend coordination:

```text
intent does not declare compositions and no legacy --config-dir fallback
```

The current example intent under `sourceplane/orun/examples/remote-state-matrix/intent.yaml` uses an older intent shape without `compositions.sources`.

The current example workflows/docs still mention `ORUN_TOKEN` for mutable remote-state execution. The live backend's mutable execution routes are OIDC-only, so the real production conformance path must use GitHub Actions OIDC with `permissions: id-token: write`.

The current local state adapter comments say local claim always succeeds. That does not prove the spec's best-effort local locking requirement when multiple local processes share the same execution ID.

# Proposed Spec Change

Before Dashboard UI resumes, add two Task 8 remediation tasks:

1. Implement and verify a local filesystem state lock in `sourceplane/orun`.
2. Fix the remote-state matrix fixture and workflow so it exercises the live backend through GitHub Actions OIDC.
3. Add a detailed README explaining:
   - local default state testing
   - local lock/concurrency testing
   - remote-state testing through GitHub Actions
   - remote status/log inspection
   - why `ORUN_TOKEN` is not the production mutable-execution path for the current backend
4. Run and verify the conformance workflow against the live backend before Dashboard UI work resumes.

# Why This Is Needed

The backend being live is necessary but not sufficient. The CLI and examples must prove that real matrix runners can coordinate through it, and local state must be safe enough for development workflows.

Without this, Dashboard UI would be built on top of an unproven execution path.

# Impacted Files / Tasks

Likely `sourceplane/orun` files:

- `examples/remote-state-matrix/intent.yaml`
- `examples/remote-state-matrix/README.md`
- `examples/github-actions/remote-state-matrix.yml`
- `.github/workflows/remote-state-conformance.yml`
- `website/docs/examples/remote-state-matrix.md`
- `website/docs/reference/environment-variables.md`
- `website/docs/cli/orun-run.md`
- `internal/state/**`
- `internal/statebackend/**`
- `internal/runner/**`
- relevant tests under `cmd/orun`, `internal/state`, or `internal/statebackend`

Likely `sourceplane/orun-backend` files:

- `ai/state.json`
- Task/report files

# Compatibility / Migration Notes

- Local state remains the default.
- Remote mutable execution remains OIDC-first for the live backend.
- `ORUN_TOKEN` can remain as a client fallback for backends that explicitly accept a bearer token, but docs must not imply the current production backend accepts it for claim/update/log upload.
- Do not broaden backend auth just to make local remote-state testing easier.

# Recommendation

Accept immediately.

Insert Task 0008.4 and Task 0008.5 before Task 0009. Resume Dashboard UI only after remote-state conformance is green against the live backend.
