# Task ID

task-0019-full-system-verification

# Agent

Verifier

# Current Repo Context

Tasks 0001 through 0018 are implemented, and Task 0018 is verified PASS on
`main`:

- Latest main commit at planning time: `faf9e40`
- Task 0018 merge commit: `db0e5f0`
- Live Worker: `https://orun-api.sourceplane.ai`
- Live Worker workers.dev fallback:
  `https://orun-api.rahulvarghesepullely.workers.dev`
- Live Dashboard: `https://orun-dashboard.sourceplane.ai`
- Stack version: `oci://ghcr.io/sourceplane/stack-tectonic:0.12.0`
- Cloudflare queue resources from Task 0018:
  - `orun-catalog-ingest`
  - `orun-catalog-ingest-dlq`

The repo is still marked yellow, mostly because the original Task 0012 local
remote-state conformance verifier report was FAIL and no Task 0012.1 verifier
report exists, even though later follow-ups passed. This task is the thorough
release-grade verification pass for everything currently implemented.

# Objective

Perform a full-system verification of the implemented Orun backend, dashboard,
Cloudflare resources, queue-backed catalog ingestion, CI/CD wiring, and
sourceplane/orun CLI integration.

This is a verification and evidence task. Do not implement new product behavior.
Small verifier-only fixes are allowed only for reports or obviously broken test
harness/scripts. For behavioral, security, persistence, roadmap, or API drift,
write a proposal and report the blocker.

The final output should answer:

- What is production-ready now?
- What is implemented but failing?
- What is implemented but unverified?
- What is intentionally deferred?
- What should Task 0020 do next?

# Read First

## Compact Context

- `ai/context/current.md`
- `ai/context/task-ledger.md`
- `ai/context/decisions.md`
- `ai/context/open-risks.md`
- `ai/state.json`
- `SCHEDULE.md`
- `README.md`

## Latest Reports

- `ai/reports/task-0018-verifier.md`
- `ai/reports/task-0018-implementer.md`
- `ai/reports/task-0017-verifier.md`
- `ai/reports/task-0016-verifier.md`

Use the compact ledger for older tasks. Only unpack
`ai/archive/tasks-reports-20260508.tar.gz` if code/specs/current context are
insufficient to understand a specific historical claim.

## Specs

- `spec/00-constitution.md`
- `spec/01-monorepo-structure.md`
- `spec/02-devops.md`
- `spec/03-types-package.md`
- `spec/04-worker-api.md`
- `spec/05-coordinator-do.md`
- `spec/06-auth.md`
- `spec/07-storage.md`
- `spec/08-account-repo-linking.md`
- `spec/09-cli-integration.md`
- `spec/10-rate-limiting.md`
- `spec/11-dashboard-ui.md`
- `spec/12-catalog-index.md`

## Open Proposals

- `ai/proposals/task-0008.2-deployment-blocker.md`
- `ai/proposals/task-0008.4-remote-state-conformance.md`
- `ai/proposals/task-0009-spec-update.md`
- `ai/proposals/task-0010-device-flow-rate-limiting.md`
- `ai/proposals/task-0011-cloud-link-api.md`
- `ai/proposals/task-0011-orun-token-precedence.md`
- `ai/proposals/task-0016-spec-update.md`

## Relevant Code Areas

Use `rg --files` to discover exact files. At minimum inspect:

- `.github/workflows/workflow.yml`
- `.github/workflows/task0018-provision-smoke.yml`
- `apps/worker/wrangler.jsonc`
- `apps/worker/src/index.ts`
- `apps/worker/src/storage.ts`
- `apps/worker/src/handlers/**`
- `apps/worker/src/auth/**`
- `apps/dashboard/**`
- `packages/types/src/**`
- `packages/storage/src/**`
- `packages/client/src/**`
- `packages/coordinator/src/**`
- `migrations/**`
- sibling repo `/Users/irinelinson/sourceplane/orun` for CLI auth,
  remote-state, and backend bootstrap verification

## External Docs To Check Before Cloudflare Verification

- Wrangler Queues commands:
  https://developers.cloudflare.com/workers/wrangler/commands/queues/
- Queue producer/consumer config:
  https://developers.cloudflare.com/queues/configuration/configure-queues/
- Queues API list/consumer response:
  https://developers.cloudflare.com/api/resources/queues/methods/list/
- Cloudflare Workers custom domains, routes, D1, R2, Durable Objects, Pages, and
  secrets docs as needed for resource verification.

# Required Outcomes

## 1. Establish The Exact Verification Target

- Checkout `main` and fast-forward from `origin/main`.
- Record:
  - local branch
  - `git rev-parse HEAD`
  - latest Task 0018 commit present on main
  - any dirty worktree files before verification begins
- Do not revert unrelated dirty files. If dirty files affect verification,
  describe them in the report.
- Confirm no open PR is required before this verification pass.

## 2. Local Monorepo Quality Gates

Run from `/Users/irinelinson/sourceplane/orun-backend`:

```bash
pnpm install --frozen-lockfile
pnpm exec turbo run typecheck
pnpm exec turbo run test
pnpm exec turbo run build
```

Also run targeted suites:

```bash
pnpm --filter @orun/worker exec vitest run
pnpm --filter @orun/dashboard exec vitest run
pnpm --filter @orun/storage exec vitest run
pnpm --filter @orun/client exec vitest run
pnpm --filter @orun/coordinator exec vitest run
```

Record task counts, test counts, failures, and cached vs executed tasks.

## 3. Delivery And CI/CD Verification

Verify the current CI/CD path, not just local scripts:

- inspect the latest successful and failing GitHub Actions runs on `main`
- inspect logs, not only status summaries
- confirm `workflow.yml` runs Orun plan and changed jobs through stack-tectonic
- confirm `kiox -- orun plan --changed` appears in review/plan logs where
  expected
- confirm `kiox -- orun run --changed` appears in build/deploy logs where
  expected
- confirm Worker deploy dry-runs expose all expected bindings
- confirm dashboard deploy checks run when dashboard files change

Run locally:

```bash
/Users/irinelinson/.local/bin/kiox -- orun plan --changed
/Users/irinelinson/.local/bin/kiox -- orun run --changed
```

If no changed jobs exist, record that. If the command deploys or mutates live
resources, make sure the target environment is intentional before proceeding.

## 4. Cloudflare Resource Inventory

Use Cloudflare CLI/API with:

- `CLOUDFLARE_ACCOUNT_ID`
- `CLOUDFLARE_API_TOKEN`

Do not print token values. Verify the live Cloudflare account backing
`orun-api.sourceplane.ai` has exactly the implemented resource set:

### Worker

- Worker script `orun-api` exists.
- Worker latest deployed version matches or post-dates the verified main commit.
- Custom domain `orun-api.sourceplane.ai` routes to the same Worker.
- workers.dev hostname is available as fallback.
- Worker has expected bindings:
  - Durable Objects: `COORDINATOR`, `RATE_LIMITER`
  - R2: `STORAGE` -> `orun-storage`
  - D1: `DB` -> `orun-db`
  - Queue producer: `CATALOG_INGEST_QUEUE` -> `orun-catalog-ingest`
  - vars: `GITHUB_JWKS_URL`, `GITHUB_OIDC_AUDIENCE`
  - expected secrets are present by name, not value
- Cron trigger `*/15 * * * *` is configured.
- No production `DB_CATALOG_0`, `DB_CATALOG_1`, `CATALOG_SHARD_*`, Hyperdrive,
  Postgres, or one-D1-per-tenant bindings are active.

### Queues

Verify via CLI and Cloudflare API:

- `orun-catalog-ingest` exists.
- `orun-catalog-ingest-dlq` exists.
- `orun-catalog-ingest` has one intended consumer for `orun-api`.
- producer count is nonzero and corresponds to `orun-api`.
- consumer settings match Task 0018:
  - batch size 10
  - max retries 3
  - max wait/batch timeout 30s / 30000ms
  - DLQ `orun-catalog-ingest-dlq`
- no unexpected extra consumer is attached.
- DLQ depth/state is inspected. If messages exist, sample metadata safely and
  record whether they are expected smoke leftovers or real failures. Do not dump
  raw payloads into the report.

### D1

- D1 database `orun-db` exists.
- migrations through `0006_tenant_routes.sql` are applied remotely.
- key tables exist for runs/jobs/namespaces/accounts/CLI sessions/catalog.
- catalog tables contain the Task 0018 smoke upload/component rows.
- no shard D1 bindings are active until `task-0016-spec-update.md` is resolved.

### R2

- R2 bucket `orun-storage` exists.
- expected namespace-prefixed paths exist for at least one run/log or catalog
  envelope/component-state smoke artifact.
- object paths do not leak account IDs or use mutable names as primary keys.

### Durable Objects

- `RunCoordinator` and `RateLimitCounter` classes are deployed with expected
  migrations.
- Scheduled cleanup does not throw in logs.

### Pages

- Pages project for `orun-dashboard` exists.
- `https://orun-dashboard.sourceplane.ai` serves the latest dashboard build.
- dashboard API base points to the intended live Worker.

## 5. Live Backend Endpoint Matrix

Run endpoint checks from a local machine and, where OIDC is required, from a
GitHub Actions workflow with `id-token: write`. Record hostnames separately:

- custom domain: `https://orun-api.sourceplane.ai`
- workers.dev fallback:
  `https://orun-api.rahulvarghesepullely.workers.dev`

### Public/Auth-Guard Checks

- unknown path returns a typed error envelope.
- unauthenticated mutable run route returns `401` or `403`.
- unauthenticated catalog read returns `401` or `403`.
- dashboard OAuth start route returns an expected redirect.
- device-flow endpoints reject malformed requests safely.
- CORS/returnTo behavior does not allow arbitrary origins.

### GitHub OIDC Checks

Use a GitHub Actions workflow with OIDC audience `orun`.

Verify:

- `POST /v1/runs` succeeds for a valid repo OIDC token.
- job claim/update/heartbeat/status/log flow works for the created run.
- duplicate claim behavior is deterministic and safe.
- cross-namespace OIDC access is rejected.
- `POST /v1/catalog/sync` returns `202`.
- queue delivery eventually normalizes the catalog component.
- malformed catalog envelopes are synchronously rejected before enqueue.
- queue messages remain R2-reference-only, not full envelopes.

You may reuse `.github/workflows/task0018-provision-smoke.yml` for the queue
resource and catalog sync portion. Dispatch it from `main`, not an old task
branch:

```bash
gh workflow run task0018-provision-smoke.yml -f branch=main
```

Inspect the resulting logs and record the run URL.

### Session/CLI Token Checks

Use a valid dashboard or CLI session token without printing it.

Verify:

- `GET /v1/accounts/me`
- `GET /v1/accounts/repos`
- `POST /v1/accounts/repos/link` or the currently implemented link endpoint path
- `GET /v1/catalog/components`
- `GET /v1/catalog/components/:id`
- `GET /v1/catalog/components/:id/history`
- `GET /v1/catalog/components/:id/dependencies`
- `GET /v1/catalog/components/:id/runs`
- dashboard sessions cannot call mutable coordination routes
- CLI sessions can perform intended local remote-state mutation routes only under
  local namespaces
- local namespaces do not appear in catalog reads

## 6. sourceplane/orun CLI Verification

In sibling repo `/Users/irinelinson/sourceplane/orun`, verify implemented CLI
features that integrate with this backend:

```bash
git status --short -uall
go test ./...
go test -race ./...
go vet ./...
```

Verify local auth and remote-state flows:

- `orun auth login` or `orun auth login --device`
- `orun auth status`
- `orun auth token --audience orun-backend`
- `orun cloud link`
- local `orun run --remote-state` against
  `https://orun-api.sourceplane.ai`
- duplicate local process claim behavior
- dependency waiting through `/runnable`
- `orun status --remote-state`
- `orun logs --remote-state`

This section should close or keep open the Task 0012 yellow bookkeeping risk.
If local remote-state conformance cannot be run, report the exact blocker.

Verify backend bootstrap:

- `orun backend init/status/destroy` help and dry-run behavior.
- If Cloudflare credentials are available, run a disposable self-hosted backend
  smoke using a unique prefix and destroy it afterward.
- Confirm whether bootstrap provisions queues. Current context says queue
  provisioning is not wired into `orun backend init`; verify and record this as
  either a blocker or a follow-up depending on what docs/specs claim.
- Verify no Cloudflare/API/session secrets are written to logs or config files.

## 7. Dashboard Verification

Verify both code and live UI:

- `pnpm --filter @orun/dashboard exec vitest run`
- `pnpm --filter @orun/dashboard exec vite build` or repo-equivalent build
- live dashboard loads at `https://orun-dashboard.sourceplane.ai`
- OAuth login entrypoint starts correctly
- authenticated dashboard shows:
  - Catalog as default view
  - Runs
  - Repositories
  - Settings/session/account view
  - component detail
  - run detail/log viewer
- dashboard only reads visible linked repo namespaces
- local namespaces are excluded from catalog UI
- responsive visual QA at 1440px, 768px, and 390px
- no broken layout, overlapping controls, unreadable text, or console errors

Use browser automation or Playwright screenshots if available. Include screenshot
paths only if they are useful evidence.

## 8. Security And Data Boundary Review

Review code and live behavior for:

- no plaintext refresh tokens stored in D1
- GitHub OAuth/PAT tokens are not stored or returned
- JWT/session secrets are never logged
- catalog queue/drop logs use safe opaque reason codes
- component paths and raw envelopes are not logged
- OIDC canonical repo namespaces remain separate from local CLI namespaces
- dashboard sessions remain read-oriented
- CLI sessions cannot write canonical repo state
- D1 is treated as derived index, not coordinator source of truth
- R2 paths are namespace-prefixed
- queue messages are pointer-only
- rate limiting still protects auth/session/device endpoints according to the
  current implemented contract

Run a repository secret scan with local tools available, for example:

```bash
rg -n "BEGIN (RSA|OPENSSH|PRIVATE)|CLOUDFLARE_API_TOKEN|ORUN_SESSION_SECRET|GITHUB_CLIENT_SECRET|refresh_token|access_token" .
```

Inspect findings carefully; test names and docs may contain safe placeholders.

## 9. Storage And Scalability Contract Review

Verify scalable design choices are represented honestly:

- current production is still single `DB` for core and catalog/run fallback
- storage router exists and catalog handlers do not hardcode catalog work to
  `env.DB`
- queue-backed catalog ingestion is active
- multi-shard catalog D1 is not active
- `ai/proposals/task-0016-spec-update.md` remains the blocker for activating
  `DB_CATALOG_0` / `DB_CATALOG_1`
- no code path introduces one-D1-per-tenant Worker bindings
- docs/specs do not overclaim that sharded catalog D1 is production-ready

If specs/docs and code disagree, either classify the mismatch as docs drift or
create a spec proposal.

## 10. Report, Classification, And Next Task

Write `ai/reports/task-0019-verifier.md`.

The report must include:

- Result: `PASS`, `FAIL`, or `PARTIAL`
- Verified commits/repos
- Local backend checks
- CI/CD checks
- Cloudflare resource inventory
- Live endpoint matrix
- CLI/sourceplane-orun verification
- Dashboard verification
- Security/data-boundary review
- Known deferred items
- Issues with severity:
  - P0: security/data-loss/system unusable
  - P1: implemented core feature broken or live resource missing
  - P2: important production-readiness gap with workaround
  - P3: docs/observability/cleanup gap
- Spec proposals opened or recommended
- Recommended Task 0020

PASS means every implemented claim works locally and live, and any remaining
gaps are explicitly out of scope, documented, and not blocking current use.

PARTIAL means the implementation appears sound but one or more verification
surfaces could not be exercised, such as missing local auth, missing Cloudflare
permission, or unavailable browser session.

FAIL means an implemented user-facing or production-critical claim is broken,
unsafe, or materially unverifiable.

# Constraints

- Do not enable multi-shard D1 bindings during verification.
- Do not mutate or delete production data except safe smoke records with unique
  IDs.
- Do not purge queues or DLQs.
- Do not print secrets, OIDC tokens, session tokens, raw envelopes, or raw queue
  messages in reports.
- Do not change production WAF/security settings unless the user explicitly asks.
- Do not treat known unimplemented future scope as a failure unless docs/specs
  claim it is implemented.
- Do not unpack the historical archive unless compact context and code are
  insufficient.

# Acceptance Criteria

- `ai/reports/task-0019-verifier.md` exists.
- Backend local tests/typecheck/build results are recorded.
- Main CI logs are inspected and summarized.
- Cloudflare Worker, D1, R2, Durable Object, Queue, and Pages resources are
  inventoried.
- Live backend endpoint matrix includes unauthenticated, OIDC, and session/CLI
  checks.
- Queue-backed catalog sync is verified live after Task 0018.
- Dashboard live visual/auth smoke is recorded.
- sourceplane/orun CLI auth, remote-state, and bootstrap verification is
  recorded or precisely blocked.
- Task 0012 yellow bookkeeping is either closed with evidence or kept open with
  a concrete blocker.
- The report recommends the next highest-leverage Task 0020.

# When Done Report

Write only the verifier report; do not open a PR unless you made verifier-only
file changes that need to be preserved. If you do create a PR, it must contain
only verification reports/proposals/harness fixes, not product behavior changes.
