# Task 0008.3

# Agent

Implementer

# Current Repo Context

This is the second high-priority live deployment blocker before Task 0009 Dashboard UI.

Task 0008.2 must complete first by releasing a stack-tectonic version that supports the live Cloudflare deployment shape needed by `orun-backend`.

Current known deployment problems from orchestration:

- `sourceplane/orun-backend` `main` push run `25252826186` succeeded with zero jobs:

  ```text
  0 components x 3 envs -> 0 jobs
  mode: changed-only
  ```

- Task 0007 merge push run `25249698677` also produced zero jobs.
- PR runs validated builds/dry-runs but did not live deploy.
- `gh secret list --repo sourceplane/orun-backend --json name,updatedAt,visibility` returned `[]`.
- `gh variable list --repo sourceplane/orun-backend --json name,value,updatedAt` returned `[]`.
- Local `wrangler whoami` found an account, but live API operations failed with `Invalid access token [code: 9109]`.
- `apps/worker/wrangler.jsonc` contains a placeholder D1 database ID.

Primary target repo:

```text
/Users/irinelinson/sourceplane/orun-backend
```

Related repos:

```text
/Users/irinelinson/sourceplane/stack-tectonic
/Users/irinelinson/sourceplane/orun
```

Do not proceed until Task 0008.2 identifies the new stack version to consume.

# Objective

Consume the new stack-tectonic release, provision the actual Cloudflare resources for `orun-backend`, deploy the Worker live, apply database migrations, configure required secrets/vars, and verify the live API and remote-state path.

Only after this task passes should Task 0009 Dashboard UI resume.

# Read First

Read:

1. `/Users/irinelinson/sourceplane/orun-backend/ai/proposals/task-0008.2-deployment-blocker.md`
2. `/Users/irinelinson/sourceplane/orun-backend/ai/reports/task-0008.2-implementer.md`
3. `/Users/irinelinson/sourceplane/orun-backend/spec/00-constitution.md`
4. `/Users/irinelinson/sourceplane/orun-backend/spec/02-devops.md`
5. `/Users/irinelinson/sourceplane/orun-backend/spec/04-worker-api.md`
6. `/Users/irinelinson/sourceplane/orun-backend/spec/06-auth.md`
7. `/Users/irinelinson/sourceplane/orun-backend/spec/07-storage.md`
8. `/Users/irinelinson/sourceplane/orun-backend/spec/09-cli-integration.md`
9. `/Users/irinelinson/sourceplane/orun-backend/intent.yaml`
10. `/Users/irinelinson/sourceplane/orun-backend/kiox.yaml`
11. `/Users/irinelinson/sourceplane/orun-backend/kiox.lock`
12. `/Users/irinelinson/sourceplane/orun-backend/.github/workflows/workflow.yml`
13. `/Users/irinelinson/sourceplane/orun-backend/apps/worker/component.yaml`
14. `/Users/irinelinson/sourceplane/orun-backend/apps/worker/wrangler.jsonc`
15. `/Users/irinelinson/sourceplane/orun-backend/apps/worker/src/index.ts`
16. `/Users/irinelinson/sourceplane/orun-backend/apps/worker/src/router.ts`
17. `/Users/irinelinson/sourceplane/orun-backend/apps/worker/src/auth/index.ts`
18. `/Users/irinelinson/sourceplane/orun-backend/migrations/0001_init.sql`
19. `/Users/irinelinson/sourceplane/orun-backend/migrations/0002_namespaces_account.sql`
20. Inspect `/Users/irinelinson/sourceplane/orun` only as needed for live remote-state conformance; do not edit it unless the smoke workflow setup requires a narrow configuration change.
21. `/Users/irinelinson/sourceplane/orun/examples/github-actions/remote-state-matrix.yml`
22. `/Users/irinelinson/sourceplane/orun/.github/workflows/remote-state-conformance.yml`

# Required Outcomes

## 1. Consume New stack-tectonic Release

Update `intent.yaml` to use the stack version released by Task 0008.2, for example:

```yaml
ref: oci://ghcr.io/sourceplane/stack-tectonic:0.12.0
```

Run local planning to prove the stack is consumable:

```bash
/Users/irinelinson/.local/bin/kiox -- orun plan --changed
/Users/irinelinson/.local/bin/kiox -- orun plan --all
```

If `kiox.lock` changes as part of provider resolution, include and explain it.

## 2. Fix Production Workflow Semantics

Update the backend workflow or component wiring so a production `main` push cannot silently pass with zero deploy jobs when a deploy is expected.

Requirements:

- PRs should continue to run changed-component review planning.
- Production branch deploys must run a reliable deploy plan for the Worker.
- The workflow should make logs clear when there are no deployable changes versus when a deploy was intentionally run.
- Do not add a custom non-stack deployment workflow. Keep the workflow based on kiox/orun and stack-tectonic.

Potential acceptable pattern:

- PR: `kiox -- orun plan --changed`
- Push to `main`: run all production deployable jobs, or run changed with correct base/head refs if proven.

Use the Task 0008.2 stack guidance.

## 3. Provision Cloudflare Resources

Use Wrangler and/or the new stack hooks to ensure real resources exist:

- Worker script for `orun-api`
- Durable Object classes:
  - `RunCoordinator`
  - `RateLimitCounter`
- R2 bucket:
  - `orun-storage`
- D1 database:
  - `orun-db`
- D1 migrations:
  - `0001_init.sql`
  - `0002_namespaces_account.sql`
- Cron trigger:
  - `*/15 * * * *`

Replace the placeholder D1 database ID in `apps/worker/wrangler.jsonc` with the real D1 database ID if Wrangler requires it. Database IDs are not secrets.

If any resource already exists, treat creation as idempotent and verify the existing resource matches the expected name/binding.

Suggested commands, adjusted for current Wrangler:

```bash
wrangler whoami
wrangler d1 list
wrangler d1 create orun-db
wrangler r2 bucket list
wrangler r2 bucket create orun-storage
wrangler d1 migrations apply orun-db --remote --config apps/worker/wrangler.jsonc
wrangler deploy --config apps/worker/wrangler.jsonc
wrangler deployments list --config apps/worker/wrangler.jsonc
```

If local Wrangler auth is invalid, resolve it before proceeding. If it cannot be resolved non-interactively, stop and report the exact blocker; do not fake live deployment.

## 4. Configure Secrets and Vars

Configure GitHub Actions deployment secrets and Cloudflare Worker secrets.

GitHub Actions secrets required for CI deploy:

- `CLOUDFLARE_ACCOUNT_ID`
- `CLOUDFLARE_API_TOKEN`

Cloudflare Worker secrets required or strongly recommended:

- `ORUN_SESSION_SECRET`
- `ORUN_DEPLOY_TOKEN`
- `GITHUB_CLIENT_ID` if GitHub OAuth should work now
- `GITHUB_CLIENT_SECRET` if GitHub OAuth should work now

Worker vars already in `wrangler.jsonc`:

- `GITHUB_JWKS_URL`
- `GITHUB_OIDC_AUDIENCE`

Use secure generation for local secrets if no existing values are provided:

```bash
openssl rand -base64 48
```

Do not commit secret values. Do not print secret values in reports.

If GitHub OAuth app credentials are not available, deploy the Worker anyway but document that OAuth/dashboard login remains blocked until they are configured.

## 5. Live Deploy

Deploy through the stack path and, if needed, Wrangler direct deployment during remediation.

The final accepted state must be stack-driven:

- The repo's CI workflow should use the new stack version.
- A push/dispatch run should show real production deploy jobs.
- Direct Wrangler deployment can be used for initial bootstrap, but it must not be the only path left behind.

Required evidence:

- `wrangler deployments list --config apps/worker/wrangler.jsonc` shows the latest Worker deployment.
- GitHub Actions deploy run shows nonzero Worker deploy jobs.
- Logs show the live deploy step actually ran for production, not only dry-run or skip.

## 6. Live API Verification

Verify the live API endpoint.

Minimum:

```bash
curl -fsS https://<worker-url>/
```

Expected response:

```json
{ "status": "ok", "service": "orun-api" }
```

Also verify:

- CORS preflight returns expected headers.
- Unauthorized run/job API request returns typed JSON `UNAUTHORIZED`, not a Worker runtime error.
- D1 schema exists remotely. Use `wrangler d1 execute` with safe read-only SQL:

  ```sql
  SELECT name FROM sqlite_master WHERE type = 'table' ORDER BY name;
  ```

- R2 bucket exists and is accessible by Wrangler.

## 7. Remote-State End-to-End Verification

Prove the deployed backend is usable by `sourceplane/orun` remote-state behavior.

Preferred:

- Set or update `ORUN_BACKEND_URL` as a GitHub repository variable in `sourceplane/orun`.
- Trigger the gated remote-state conformance workflow in `sourceplane/orun`.
- Confirm it uses GitHub Actions OIDC against the deployed backend.
- Confirm status/log verification succeeds.

Alternative if the conformance workflow cannot run:

- Create a minimal GitHub Actions workflow dispatch or temporary branch PR that uses OIDC and `orun run --remote-state` against the deployed backend.
- Do not leave temporary workflows behind unless they are intended to be permanent.

Required evidence:

- Workflow URL and run ID.
- Backend URL used.
- Confirmation that job claim/update/log/status paths succeeded live.

## 8. Docs, Specs, and State

Update docs/specs to reflect reality:

- `spec/02-devops.md`
- `SCHEDULE.md`
- `README.md` if needed
- `ai/state.json`

State should show:

- Dashboard Task 0009 remains blocked until verification passes.
- Repo health should be `green` only after live deployment and remote-state smoke pass.
- Notes should include the live Worker URL and stack version, but no secrets.

Write the implementer report:

```text
/Users/irinelinson/sourceplane/orun-backend/ai/reports/task-0008.3-implementer.md
```

# Constraints

- Do not proceed without the stack release from Task 0008.2.
- Do not commit secrets.
- Do not claim deploy success based on dry-run, PR validation, or `0 jobs`.
- Do not leave `database_id: "PLACEHOLDER"` in live Worker config.
- Do not broaden backend auth to make smoke tests easier.
- Do not continue Dashboard UI work in this task.
- Do not delete or commit `/Users/irinelinson/sourceplane/orun/examples/remote-state-matrix/.orun/`.

# Acceptance Criteria

This task is complete when:

1. `orun-backend` consumes the new stack-tectonic release.
2. The backend production workflow cannot silently green-pass expected deploys with zero jobs.
3. Cloudflare D1, R2, Worker, Durable Objects, cron, vars, and required secrets are configured.
4. D1 migrations are applied remotely.
5. The Worker is deployed live.
6. The live root endpoint returns `{ "status": "ok", "service": "orun-api" }`.
7. Unauthorized live API requests return typed JSON errors.
8. Remote D1 schema and R2 bucket are verified.
9. GitHub Actions logs prove a real production deploy ran.
10. A live remote-state end-to-end smoke/conformance run succeeds, or a precise blocker is reported.
11. A PR is opened and merged in `sourceplane/orun-backend`.
12. Local `main` is synced after merge.
13. The report is written at:

```text
/Users/irinelinson/sourceplane/orun-backend/ai/reports/task-0008.3-implementer.md
```

# Required Checks

Run at minimum:

```bash
pnpm install
pnpm --filter @orun/worker test
pnpm --filter @orun/worker typecheck
pnpm --filter @orun/worker build
pnpm exec turbo run test typecheck build
/Users/irinelinson/.local/bin/kiox -- orun plan --changed
/Users/irinelinson/.local/bin/kiox -- orun plan --all
/Users/irinelinson/.local/bin/kiox -- orun run --changed
git diff --check
```

Run Cloudflare checks:

```bash
wrangler whoami
wrangler d1 list
wrangler r2 bucket list
wrangler deployments list --config apps/worker/wrangler.jsonc
curl -fsS https://<worker-url>/
```

Inspect GitHub Actions logs with `gh run view --log`, not only status summaries.

# When Done Report

Write:

```text
/Users/irinelinson/sourceplane/orun-backend/ai/reports/task-0008.3-implementer.md
```

Include:

```md
# Task 0008.3 Implementer Report

## Summary

## Stack Version Consumed

## Files Changed

## Cloudflare Resources

## Secrets and Vars

## Live Worker URL

## D1 Migration Evidence

## R2 Evidence

## GitHub Actions Deploy Evidence

## Remote-State Live Smoke

## Checks Run

## Assumptions

## Spec Proposals

## Remaining Gaps

## PR Number
```

Never include secret values in the report.
