# Task 0012.3 Verifier Report

Result: BLOCKED

## Summary

PR #85 (`feat: auto-resolve namespace ID for local remote-state runs`) implements the CLI-side namespace auto-resolution correctly and all local checks pass. However, verification is blocked because Task 0012.2 verifier has returned **FAIL** due to a security-sensitive authorization gap in the backend `POST /v1/accounts/repos/link` endpoint (commit `61f2ba8` removed the `allowedNamespaceIds` check without an accepted proposal or spec update). Per the Task 0012.3 verifier task spec: "If Task 0012.2 verifier FAILs due to authorization/security drift, do not PASS Task 0012.3." PR #85 must not be merged until the backend security drift is remediated and Task 0012.2 reaches PASS.

## Dependency Status

- Task 0012.2 verifier: **FAIL** — see `/Users/irinelinson/sourceplane/orun-backend/ai/reports/task-0012.2-verifier.md`.
- Blocker: `handleLinkRepoFromSession` in `apps/worker/src/handlers/accounts.ts` does not check that the resolved namespace ID is in the caller's `allowedNamespaceIds`. Any authenticated CLI user can link any known repo slug to their account.
- No accepted proposal exists relaxing this requirement.
- `spec/04-worker-api.md:96` still requires the check.

**Task 0012.3 cannot PASS until Task 0012.2 PASSes.**

## PR and CI

- PR #85: open, not draft, CLEAN merge state.
- Head SHA: `0a54b4fe060607a15c3e778dc86686452529ab4a`.
- Commits: `3ddc302`, `f1157e8`, `0a54b4f`.
- CI run `25485760811` (Orun Plan + Harness dry-run guard): both green. Live conformance jobs skipped by gate — expected for a PR workflow.
- No tokens, credentials, or secrets visible in PR metadata or CI logs.

## CLI Backend Client

`internal/cliauth/backend.go` — `LinkRepoFromSession` (lines 177-190):

- Calls `POST /v1/accounts/repos/link`. ✓
- Sends `Authorization: Bearer <accessToken>`. ✓
- Sends JSON body `{ repoFullName }`. ✓
- Parses typed backend errors into `*APIError` via `doJSON`. ✓
- No GitHub PAT support added. ✓
- `LinkRepoFromSessionResponse` struct: `NamespaceID`, `NamespaceSlug`, `LinkedAt`. ✓
- `internal/cliauth/backend_test.go`: tests cover success, `NOT_FOUND`, `FORBIDDEN`, idempotence, auth header shape, and body shape. ✓

## `orun cloud link`

`cmd/orun/command_cloud.go` — `runCloudLink`:

- `--backend-url` flag registered on `cloudCmd` (persistent). ✓
- Loads local session via `cliauth.LoadSession`. ✓
- Refreshes expired access token via `cliauth.RefreshSession` before calling backend. ✓
- Detects current GitHub remote via `resolveRepoContext` (`remote_config.go:49-70`). ✓
- Calls `client.LinkRepoFromSession` (session endpoint, no GitHub PAT). ✓
- Persists link via `persistRepoLink` → `cliauth.UpsertRepoLink` → `~/.orun/config.yaml`. ✓
- Output: `✓ linked owner/repo`, backend URL, namespace ID — no tokens printed. ✓
- Errors:
  - Not logged in → `"not logged in; run \`orun auth login\` first"`. ✓
  - No GitHub remote → `"could not detect a GitHub remote..."`. ✓
  - Backend HTTP 404 → `"backend may need updating"` hint. ✓
  - `NOT_FOUND` slug → re-login hint via `translateLinkError`. ✓
  - `FORBIDDEN` → re-auth hint. ✓
  - `UNAUTHORIZED` → login hint. ✓

Security check: CLI docs and output do not describe the relaxed backend model as a secure authorization guarantee. The link command's long description says "the Orun CLI session established by `orun auth login` is sufficient" — this is accurate and does not make a security claim about namespace ownership verification. ✓ (The security concern is in the backend, not in how the CLI documents it.)

## `orun run --remote-state`

`cmd/orun/command_run.go` — `setupRemoteStateHooks` (lines 311-347):

Auto-resolve outside GHA:
- `if namespaceID == "" && os.Getenv("GITHUB_ACTIONS") != "true"`: enters local auto-resolve path. ✓
- `ORUN_TOKEN` set without pre-cached namespace → actionable error: `"local remote-state with ORUN_TOKEN requires a pre-linked namespace; run \`orun cloud link ...\` first"`. ✓
- No GitHub remote detected → `"local remote-state requires a GitHub remote..."`. ✓
- Calls `autoResolveNamespace` (`remote_config.go:132-153`): uses `SessionTokenSource.Token()` to get/refresh access token, then calls `client.LinkRepoFromSession`. ✓
- Persists returned namespace link via `persistRepoLink`. ✓
- Passes non-empty `namespaceId` to `POST /v1/runs`. ✓
- Raw backend error `"Session creates require namespaceId"` is no longer the primary user-facing message. ✓
- Missing-login errors: delegated to `translateLinkError` with `UNAUTHORIZED` → `"run \`orun auth login\` first"`. ✓

Cache-first behavior:
- `resolveRepoContext` checks `cliauth.FindRepoLink` before calling backend. ✓
- On cache hit, no backend link call is made. ✓

Stale `allowedNamespaceIds` handling:
- `remotestate/auth.go:191-198`: if namespace not in JWT claims, checks `cliauth.FindRepoLinkByNamespaceID` as a compensating control. If cached link exists, proceeds without error. ✓
- This is deliberate and the comment acknowledges it: "cached link confirms this namespace; JWT claims may lag a re-login." ✓

## GHA and ORUN_TOKEN Behavior

- `isGitHubActionsOIDC()` (`remotestate/auth.go:220-224`): requires `GITHUB_ACTIONS=true` + both `ACTIONS_ID_TOKEN_REQUEST_*` vars. ✓
- GHA OIDC path: no local session loaded, no config lookup, no link endpoint called. ✓
- `TestOIDCPathDoesNotRequireLocalSession` (`remotestate/auth_test.go`): passes. ✓
- `ORUN_TOKEN` path: `ResolvedMode="static"`, passes through pre-supplied `NamespaceID`, does not call session-only link endpoint. ✓
- `TestOrunTokenPathNamespacePassThrough` (`remotestate/auth_test.go`): passes. ✓
- `ORUN_TOKEN` without pre-cached namespace and outside GHA: actionable error in `setupRemoteStateHooks`. ✓

## Docs and Harness

**Docs** — correctly updated:

- `website/docs/cli/orun-run.md`: happy path is `orun auth login` then `orun run --remote-state`. `orun cloud link` described as optional. GitHub Actions uses OIDC. Local uses CLI session + Git remote. ✓
- `website/docs/cli/orun-cloud.md`: auto-resolve vs explicit link documented. No GitHub PATs. `ORUN_TOKEN` caveat present. Error table covers `NOT_FOUND`, `FORBIDDEN`, `UNAUTHORIZED`, `HTTP_404`. Stale session / re-login guidance present. ✓

**Harness** — stale preflight:

- `examples/remote-state-matrix/run-local-harness.sh:150-158`: still checks `orun cloud status` output for "(not linked)" and fails the script if the repo isn't pre-cached. This predates auto-resolution and is inconsistent with Task 0012.3's requirement that `orun cloud link` is optional.
- This is a **minor issue** (not a security issue) but should be remediated in Task 0012.3.1 alongside the backend fix. A user who has never run `orun cloud link` will see the harness fail at the preflight check rather than proceeding to auto-resolve.
- `f1157e8` (docs: remove stale FORBIDDEN row from orun-cloud link error table) is correctly scoped.

## Live or Dry-Run Evidence

Tests pass locally; live auth session not available in verifier context to exercise the full auto-resolve path against `orun-api.sourceplane.ai`. The backend live smoke confirms the endpoint is up and requires auth. Because Task 0012.2 is FAIL, a positive live smoke confirming the authorized link path would still not unblock this task.

If Task 0012.2 is remediated and re-verified, recommended live evidence:

```bash
cd /Users/irinelinson/sourceplane/orun
go build -o ./orun ./cmd/orun
./orun auth status --backend-url https://orun-api.sourceplane.ai
./orun run --remote-state -i examples/remote-state-matrix/intent.yaml --dry-run \
  --backend-url https://orun-api.sourceplane.ai
```

Expected: no `"Session creates require namespaceId"` error; either a successful dry-run or a clear auth/namespace error from the link endpoint.

## Checks Run

| Check | Result |
|---|---|
| `go test ./...` (orun) | PASS (all packages) |
| `go vet ./...` (orun) | PASS |
| `git diff --check` (orun) | PASS |
| `go build -o ./orun ./cmd/orun` | not run (would pass — all tests cached clean) |
| CI run 25485760811 (PR #85 head) | green (Orun Plan + harness dry-run guard) |
| PR merge state | CLEAN |
| Token exposure in CI logs | none |
| Task 0012.2 dependency gate | **FAIL** → blocks this task |

## Issues

1. **[BLOCKER]** Task 0012.2 FAIL — backend `POST /v1/accounts/repos/link` lacks `allowedNamespaceIds` check. PR #85 must not be merged until Task 0012.2 PASSes.

2. **[MINOR — harness]** `examples/remote-state-matrix/run-local-harness.sh:150-158` still requires a pre-linked repo in config (preflight fails with "Repo linkage required" if no cached namespace). Auto-resolve is implemented in `orun run` but the harness bypasses it with an early failure. This should be fixed in Task 0012.3.1.

## Risk Notes

- The CLI implementation in PR #85 is technically correct — it auto-resolves via the link endpoint, caches the result, and populates `namespaceId` for run creation. The quality of that feature depends on the backend authorization model in Task 0012.2.
- If the backend accepts Option B (relax `allowedNamespaceIds` with compensating controls), the CLI behavior in PR #85 is appropriate as-is. If the backend restores Option A (check), the CLI behavior still works correctly — the backend will return `NOT_FOUND` for stale sessions, and `translateLinkError` maps that to a re-login message.
- The stale-namespace compensating control in `remotestate/auth.go:191-198` (accept D1 cached link when JWT claims lag) is intentional and safe provided the backend enforces ownership at link time (Option A).

## Spec Proposals

None required for Task 0012.3 CLI itself. Task 0012.2.1 should produce:

- `ai/proposals/task-0012.2-link-auth-model.md` — covering Option A vs Option B with rationale.
- Update `spec/04-worker-api.md:96` to match whichever option is accepted.

## Recommended Next Move

1. **Do not merge PR #85.**
2. Open Task 0012.2.1 remediation (restore `allowedNamespaceIds` check OR accept relaxed model with accepted proposal + spec update).
3. After Task 0012.2.1 merges and Task 0012.2 is re-verified to PASS, re-run Task 0012.3 verification.
4. Include harness fix for `run-local-harness.sh` preflight in Task 0012.3.1 (or add to Task 0012.2.1 scope).
